#include <iostream>
    #include <string>
    using namespace std;
    
    int main() {
      string txt = "teh best of the best";
      cout << "The length of the txt string is: " << txt.length();
      return 0;
    }


     def compte_mots(phrase):
     prec, nb_mots = ' ', 0
     for char in phrase:
         nb_mots += int(prec == ' ' and char != ' ')
         prec = char
     return nb_mots

      compte_mots("   salut je    suis   une phrase.   ")


      Var
    s: string;
    cons, voy, blanc, i: entier;
 
 
Fin.
    begin
    cons := 0;
    voy := 0;
    blanc := 0;
    
    for i :=1 to lengthè(s) do
      case s[i] do
      value('a' Ou 'e' Ou 'i' Ou 'u' Ou 'o' Ou 'y') do 
        voy := voy + 1;
      value('_') do 
      blanc := blanc + 1;
      else
          cons := cons + 1;
  endcase
endfor
end.
    